import fs from 'node:fs';
const html=fs.readFileSync(new URL('../index.html',import.meta.url),'utf8');
const app=fs.readFileSync(new URL('../js/app.js',import.meta.url),'utf8');
const css=fs.readFileSync(new URL('../styles.css',import.meta.url),'utf8');
const sw=fs.readFileSync(new URL('../sw.js',import.meta.url),'utf8');
const need=(v,m)=>{if(!v)throw new Error(m)};
need(html.includes('0.4.0 RC4-r1'),'visible RC4-r1 missing');
need(!html.includes('id="startQuickScoutBtn"'),'quick scout must not be on splash');
need(html.includes('id="quickScoutToggle"')&&html.includes('Spontan scouten'),'quick toggle missing');
need(html.includes('id="detailedQualityToggle"')&&html.includes('Detailliert scouten'),'detail toggle missing');
need(html.includes('scout-mode-toggles'),'two-toggle wrapper missing');
need(css.includes('grid-template-columns:minmax(0,1fr) minmax(0,1fr)'),'two-column toggle layout missing');
need(html.includes('<th title="Rotation">Rot.</th>'),'FE-UI9 live protocol header missing');
need(app.includes('<th title="Rotation">Rot.</th>'),'FE-UI9 review header missing');
need(app.includes("$('#quickScoutToggle').onchange=async()=>"),'quick toggle handler missing');
need(app.includes('const ready=matchConfigured();'),'render startup ready initialization missing');
need(sw.includes('v0.4.0-rc4-r1'),'RC4-r1 cache id missing');
console.log('RC4-r1 FE-SCOUT8/FE-UI9 test OK');
